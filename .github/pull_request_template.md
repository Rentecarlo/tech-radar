## Preview

<!--
Required. Paste the link that renders this branch on the radar, so reviewers can
see the change rather than read the JSON. Generate it with:

    ./scripts/radar-preview-url.sh

Check the link yourself before requesting a review: BYOR silently drops any blip
whose ring or quadrant it doesn't recognise, so a blip that never appears is the
failure you're looking for.
-->

Preview this branch on the radar: <!-- paste link here -->

## What's changing

<!-- Which blips are added, moved, or removed. For a move, say which ring it came from. -->

## Why

<!--
The reasoning is the point of the radar — the JSON is just how it's stored.
Say why this technology sits at this ring, in our context. Link to the CoP
discussion, spike, or incident behind it if there is one.
-->
